import React from 'react';
import { Card, CardHeader, CardTitle } from '../../../components/ui/card';

export default function Expenses() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>รายจ่าย (Coming Soon)</CardTitle>
      </CardHeader>
    </Card>
  );
}
